{% extends 'base.html.twig' %}

{% block content %}
  <form enctype="multipart/form-data" method="POST">
    <label for="name">Name</label>
    <input type="text" name="name" value="{{ name }}" size="40" required>
    <label for="positions">Positions</label>
    <input type="file" name="positions" accept=".csv">
    <div class="checkbox">
      <input type="checkbox" name="replace_positions" value="replace_positions"><label for="replace">Replace existing positions</label>
    </div>
    <div class="form-actions">
      <input type="submit" value="Submit">
    </div>
  </form>
{% endblock %}
