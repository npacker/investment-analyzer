const NormalizeWeightsButton = props => {
  const normalizeWeights = event => {
  }

  return <button type="button" onClick={normalizeWeights}>Normalize Weights</button>
}

export default NormalizeWeightsButton
